<%= form_for(my_comment) do |f| %>
  <% if my_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_comment.errors.count, "error") %> prohibited this my_comment from being saved:</h2>

      <ul>
      <% my_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label :my_thread_id %>
    <%= f.text_field :my_thread_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
